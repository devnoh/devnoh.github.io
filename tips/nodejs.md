# NODEJS

### Install

$ brew install node

$ brew upgrade node

### NPM

$ npm init
$ npm init --yes

$ npm install [packge name]
  (or npm i [package name])
$ npm install underscore
  (or $ npm i underscore)